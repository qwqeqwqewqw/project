<section class="py-16 sm:py-20 lg:py-15 bg-orange-50">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-start">
      <!-- Left Content -->
      <div class="space-y-6">
        <div>
          <div
            class="text-sm uppercase tracking-wider text-gray-600 mb-2 font-light"
          >
            Drop A Line
          </div>
          <h2
            class="text-3xl sm:text-4xl font-light text-gray-900 mb-6 uppercase tracking-wide"
          >
            SEND YOUR MESSAGE US
          </h2>
        </div>
        <p class="text-gray-600 leading-relaxed font-light">
          Please feel free to get in touch using the form. We'd love to hear for
          you.
        </p>
      </div>

      <!-- Contact Form -->
      <div class="bg-white p-8 rounded-none shadow-lg">
        <form class="space-y-6">
          <!-- Name and Email Row -->
          <div class="grid sm:grid-cols-2 gap-6">
            <div class="form-control">
              <input
                type="text"
                placeholder="Your Name"
                class="input w-full bg-transparent border-0 border-b border-gray-400 rounded-none focus:border-green-700 focus:outline-none pb-3 font-light placeholder:text-gray-400 text-midnight"
                required
              />
            </div>
            <div class="form-control">
              <input
                type="email"
                placeholder="Email Address"
                class="input w-full bg-transparent border-0 border-b border-gray-400 rounded-none focus:border-green-700 focus:outline-none pb-3 font-light placeholder:text-gray-400 text-midnight"
                required
              />
            </div>
          </div>

          <!-- Phone and Subject Row -->
          <div class="grid sm:grid-cols-2 gap-6">
            <div class="form-control">
              <input
                type="tel"
                placeholder="Mob. Num"
                class="input w-full bg-transparent border-0 border-b border-gray-400 rounded-none focus:border-green-700 focus:outline-none pb-3 font-light placeholder:text-gray-400 text-midnight"
              />
            </div>
            <div class="form-control">
              <input
                type="text"
                placeholder="General Enquiries"
                class="input w-full bg-transparent border-0 border-b border-gray-400 rounded-none focus:border-green-700 focus:outline-none pb-3 font-light placeholder:text-gray-400 text-midnight"
              />
            </div>
          </div>

          <!-- Message -->
          <div class="form-control">
            <textarea
              placeholder="Message"
              class="textarea w-full h-20 bg-transparent border-0 border-b border-gray-400 rounded-none focus:border-green-700 focus:outline-none resize-none pb-3 font-light placeholder:text-gray-400 text-midnight"
              required
            ></textarea>
          </div>

          <!-- Submit Button -->
          <div class="pt-6">
            <button
              type="submit"
              class="bg-gray-900 text-white px-8 py-3 text-sm uppercase tracking-wider hover:bg-green-700 transition-colors rounded-none font-light"
            >
              SEND MESSAGE
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>
