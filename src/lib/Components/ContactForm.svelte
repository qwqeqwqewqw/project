<section class="py-16 sm:py-20 lg:py-15 bg-orange-50">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid lg:grid-cols-2 gap-5 lg:gap-0 items-start">
      <!-- Left Content -->
      <div class="ml-16">
        <div>
          <h2
            class="text-3xl sm:text-5xl font-wasted text-gray-900 mb-3 text-center sm:text-left -ml-12 sm:ml-0"
          >
            Send us your message
          </h2>
          <h2
            class="font-light text-gray-900 mb-6 mr-15 text-center sm:text-left"
          >
            Have a question, suggestion, or just want to say hello? Fill out the
            form below and we'll get back to you as soon as possible. We're here
            to help!
          </h2>
        </div>
      </div>

      <!-- Contact Form -->
      <div class="bg-white p-8 rounded-none shadow-lg lg:mr-16 lg:-ml-10">
        <form class="space-y-6">
          <!-- Name and Email Row -->
          <div class="grid sm:grid-cols-2 gap-6">
            <div class="form-control">
              <input
                type="text"
                placeholder="Your Name *"
                class="input w-full bg-transparent border-0 border-b border-gray-400 rounded-none focus:outline-none pb-3 font-light placeholder:text-gray-400 text-midnight"
                required
              />
            </div>
            <div class="form-control">
              <input
                type="tel"
                placeholder="Mob. Num *"
                class="input w-full bg-transparent border-0 border-b border-gray-400 rounded-none focus:outline-none pb-3 font-light placeholder:text-gray-400 text-midnight"
                required
              />
            </div>
          </div>

          <!-- Phone and Subject Row -->
          <div class="">
            <div class="form-control">
              <input
                type="email"
                placeholder="Email Address *"
                class="input w-full bg-transparent border-0 border-b border-gray-400 rounded-none focus:outline-none pb-3 font-light placeholder:text-gray-400 text-midnight"
              />
            </div>
          </div>

          <!-- Message -->
          <div class="form-control">
            <textarea
              placeholder="Message *"
              class="textarea w-full h-20 bg-transparent border-0 border-b border-gray-400 rounded-none focus:outline-none resize-none pb-3 font-light placeholder:text-gray-400 text-midnight"
              required
            ></textarea>
          </div>

          <!-- Submit Button -->
          <div class="pt-6">
            <button
              type="submit"
              class="bg-almaris-blue text-white px-8 py-3 text-sm uppercase tracking-wider hover:bg-green-700 transition-colors rounded-none font-light"
            >
              SEND MESSAGE
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>
